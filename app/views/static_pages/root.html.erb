<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>

  window.images = {
    backsplash: "<%= image_path("backsplash.jpg") %>",
    happyhour: "<%= image_path("happyhourdrinks.png") %>"
  }
</script>

<main id="root"></main>
